[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
# pyflakes (F), pycodestyle (E/W), isort (I), bugbear (B), pyupgrade (UP), comprehensions (C4)
select = ["F", "E", "W", "I", "B", "UP", "C4"]
